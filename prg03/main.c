#include <stdio.h>

int main(void) {
    int a = 3, b = 7, c;
    // a = a + 1; a değişkeninin değerini 1 arttırır
    // ++a; a değişkenin değerini 1 arttırır
    a += 1; // a değikenin değerini 1 arttırır
    printf("a = %d\n", a);
    b *= a; // b = b * a;
    return 0;
}